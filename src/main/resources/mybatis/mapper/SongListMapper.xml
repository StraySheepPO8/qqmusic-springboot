<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--suppress MybatisMapperXmlInspection -->
<mapper namespace="com.yahoo.mapper.SongListMapper">
    <select id="selectByRecommend" resultType="songList">
        select * from qq_music.song_list where type = #{type}
    </select>
</mapper>